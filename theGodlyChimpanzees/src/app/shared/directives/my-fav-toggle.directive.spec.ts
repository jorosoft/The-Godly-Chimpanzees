import { MyFavToggleDirective } from './my-fav-toggle.directive';

describe('MyFavToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new MyFavToggleDirective();
    expect(directive).toBeTruthy();
  });
});
